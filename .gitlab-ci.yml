image: docker:19.03.12

services:
  - docker:19.03.12-dind

stages:
  - build

build-backend:
  stage: build
  tags:
    - docker
  before_script:
    - docker info
  script:
    - cd backend
    - docker build -t my-backend-image .

build-frontend:
  stage:
